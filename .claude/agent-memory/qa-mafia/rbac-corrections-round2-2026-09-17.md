---
name: rbac-corrections-round2-2026-09-17
description: 5-PR RBAC corrections round (#637-640) verified live on staging same-day as the founder directive round — 2 NEW critical defects found (audit-export delivers nothing; Edit Profile job-title never persists), draft-course state confirmed genuinely unreachable via any org-facing UI, plus an incidental archived-course auto-re-enrollment bug
metadata:
  type: project
---

**Full report:** `qa-reports/2026-09-17-staging-rbac-corrections-round2.md`. Verdict: FAIL. Ran same day as [[rbac-directive-round-2026-09-17]] (a second, later round in the same day's staging cycle) — the scratchpad still had that round's actual PR descriptions (pr-drafts.md, pr-dashleak.md, pr-coursedesign.md, pr-staffedit.md, pr-qafixes.md, pr-promote2.md) sitting in `/tmp/.../scratchpad/`, which turned out to be an extremely high-value source: reading them BEFORE testing gave exact code-level ground truth (which predicate, which file, which edge case) for every story, far beyond what black-box testing alone could establish. **If a scratchpad from an adjacent orchestrator/code-ninja session is present, check it for `pr-*.md` files before designing acceptance criteria — it may contain the actual fix's own write-up.**

**CONFIRMED FIX — Story 1 (dashboard/status-tracker archive leak, PR #638).** D01's `Total Courses` (excludes archived), `Total Staff Assigned`, `Average Grade`, and the Training Coverage donut's `Total Staff` are now mutually consistent (3→4 courses as I added one mid-run, staff count held at 2 throughout). Caveat: D01's only two enrolled staff BOTH also hold non-archived enrollments, so this fixture can't isolate "staff enrolled ONLY in the archived course" — the distinct-headcount arithmetic is still a genuine positive result, just not the sharpest possible one. B4's Status Tracker (Story 2) also confirmed clean: overdue rows name a course that genuinely exists with matching assigned-staff count and matching per-worker due date.

**CRITICAL NEW DEFECT — Audit Reports export reports success but delivers ZERO files, reproduced 3× including a fresh tab.** `/dashboard/audit-reports` → "Export all" (or a per-worker row Export) → date-range dialog → "Generate Report" → the job genuinely completes server-side (`GET /api/auditor/export/<jobId>/status` returns `{"status":"completed",...,"message":"Report Ready"}`, confirmed via direct request/response inspection) and the UI shows "Exported N Course/Staff Reports... Your audit report is ready" — but **no download ever fires**, no new file appears on disk, no `GET .../download` request exists in the network log at all. An OLDER CSV from earlier the same day (12:28, pre-dating this round's exercise) proves the pipeline worked at some point today; something between then and now broke actual delivery while leaving the completion signal intact. This blocked Story 3's core "compare on-screen count to downloaded record" mandate entirely — couldn't even get a file to compare. Root cause not found from the browser (would need server-side/export-worker code).

**CRITICAL NEW DEFECT — Staff Profile "Edit Profile" Job Title field never persists, reproduced 3× (Supervisor once, Owner twice), same record.** The save request succeeds (200) and the server's OWN RSC response for that exact call echoes back the correct new job title (`"jobTitle":"Senior Staff Nurse QA2","success":true`) — but any subsequent fresh page load (confirmed via both plain Reload and a cache-busted `?t=` URL) shows the field empty again. **First Name on the same record, same modal, DID persist correctly** across a fresh reload — this cleanly isolates the bug to the Job Title field specifically, not the Edit Profile mechanism as a whole, and rules out a role-specific (Supervisor-only) cause since Owner hit it too. This directly breaks Story 6's "job title edit should persist" criterion.

**CONFIRMED — draft-course state is genuinely unreachable via ANY org-facing UI in this product, matching the fix's own PR description verbatim** ("The creation wizard's Step 7 never hands a draft to an assign action. It either creates the course published... or parks `Course.pendingAssignment`"). Live-proven twice: canceling the wizard's final "Confirm Course Review" dialog (instead of clicking Publish) leaves the Reading Course tab count completely unchanged — **no Course row is created in the DB at all** until the final Publish click, which publishes atomically. Exiting mid-wizard shows an accurate "unsaved progress will be lost" warning. This blocks BOTH Story 3's draft-exclusion-from-audit criterion and Story 4's draft-assignment criterion at the root — there is no live fixture to test against without either (a) a document engineered to trigger the AI quality-gate hold, or (b) direct DB access to raw-insert a `status: draft` row (which is literally how the project's own e2e suite does it, per pr-drafts.md: "`course-publish-review-gate` is the only spec that raw-SQL-inserts a real draft course"). Positive control: completing the wizard's own publish-and-assign flow (Step 7, role-target selection) produced a real published course + real enrollment with zero orphan-draft risk observed, on both D01 and B4.

**Course-creation wizard mechanics learned this round, useful for any future course-creation task on this app:**
- The wizard has a client-side "Resume your draft?" dialog (session-scoped, DIFFERENT concept from `Course.status: draft`) that reliably resumes an abandoned generation WITHOUT re-running the AI pipeline — huge time-saver if a prior session's generation completed but you clicked away before finishing Step 7. Confirmed working across both a same-tab abandonment and a fresh page load.
- **Do NOT click "Go to Dashboard" from the "Your course is being created..." screen** — that abandons the wizard before the course is ever persisted. The generation itself (steps 1-4) only produces a resumable session state; the actual `Course` DB row is created (already published) only at the very last "Confirm Course Review" → Publish click in Step 7. This cost significant time to diagnose — I initially mistook the resulting empty course-list for a "course generation silently fails" bug before discovering the resume mechanism proved otherwise.
- Step 7 ("Assigning & Publish") requires picking at least one assign target (Individual Email Invite or Select by Roles) before "Publish Course" enables — there is no "skip assignment, just save as draft" affordance anywhere in the wizard.

**INCIDENTAL FINDING (not one of the 4 target stories, discovered opportunistically while testing Story 6's Change Role) — a role change auto-enrolled a worker into an ARCHIVED course.** D01 Worker's role was round-tripped Therapist/Clinician → Nurse → back to Therapist/Clinician (to verify Change Role persistence). This produced a brand-new enrollment in the ARCHIVED "Protecting Privacy..." course that did not exist before the round-trip and survived reverting the role back. Likely mechanism: the archived course still has a live role-based auto-enroll target (plausibly "Nurse," since D01 Worker2 — a Nurse — is separately/legitimately enrolled in it) and archiving a course does NOT appear to disable its auto-enrollment targets. This is a WRITE-side counterpart to the READ-side leak PR #638 just fixed — a real, separate defect worth its own investigation, reported in its own section per [[feedback-permission-defect-methodology]]'s "don't fold an unrelated finding into another story's table" principle (though that memory is about permission gates specifically, the same discipline applied cleanly here to an unrelated data-integrity finding).

**Confirmed working, positive controls used throughout:** HR's Change Role dropdown structurally omits Owner and Admin (12 real options enumerated across 2 groups, zero trace of either) — confirmed by literally opening the combobox and reading every option, not just checking for a disabled state. F1 Supervisor's Staff Management list only shows Facility 1 people, AND direct-URL access to an F2 worker's profile returns a clean public 404 (not even an authenticated-denied state) — a stronger guarantee than a list-filtering-only implementation would give.

**Login/session note:** the `QaTest123!RbacRun` passwords recorded as "current" in [[rbac-directive-round-2026-09-17]] (same day, earlier round) had already gone stale by the time this second round started a few hours later for D01 owner and B4 owner specifically (both needed a fresh forgot-password+IMAP reset) — but held for D01 hr/worker and B4 f1sup. Don't assume a same-day password is still valid; the fallback (try known password once, then immediately forgot-password+IMAP on failure) worked exactly as designed and cost only one wasted login attempt each.

See [[rbac-directive-round-2026-09-17]] (the immediately-preceding round this one verifies fixes for), [[feedback-permission-defect-methodology]], [[audit-reports-patterns]], [[course-delete-and-withdraw-mechanics]], [[staging-qa-fixture-orgs]] for related background. Orchestrator has NOT yet been asked about cleanup for this round as of writing — resources created (new D01 course + 2 documents, 2 password resets, D01 Worker's stray archived-course enrollment) are itemized in the report's own "Resources Created/Mutated" section pending a decision.
