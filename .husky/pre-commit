echo "Running pre-commit checks (lint-staged)..."

# Fast path only: format & lint the STAGED files via lint-staged
# (config in package.json). lint-staged re-stages its own fixes.
# Heavy checks (Prisma generate, typecheck, tests, build) run in
# .husky/pre-push and in CI (.github/workflows/ci.yml).
npx lint-staged || exit 1

echo "Pre-commit checks passed!"
