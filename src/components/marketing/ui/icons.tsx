// Marketing UI glyphs. Obvious interface icons map to lucide-react (per
// CLAUDE.md — no inline <svg> for UI icons). Bespoke brand/marketing
// illustration glyphs that lucide lacks stay as small components elsewhere.
export { ArrowRight } from 'lucide-react';
